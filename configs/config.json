{
  "data_config": {
    "dataset_name": "cifar-10",
    "input_shape": [32, 32, 3]
  },
  "model_config": {
    "name": "custom",
    "optimizer": "Adam",
    "batch_size": 32,
    "epochs": 5,
    "validation_split": 0.3
  }
}