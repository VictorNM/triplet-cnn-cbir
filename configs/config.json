{
  "data_config": {
    "dataset_name": "cifar-10",
    "dataset_type": "raw"
  },
  "model_config": {
    "name": "custom",
    "optimizer": "Adam",
    "batch_size": 32,
    "epochs": 10,
    "validation_split": 0.3
  }
}