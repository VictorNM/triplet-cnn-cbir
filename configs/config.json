{
  "data_config": {
    "dataset_name" : "cifar-10",
    "dataset_augmentation" : {
      "rotation_range" : 20,
      "zoom" : 20

    }

  },
  "model_config": {
    "optimizer": "Adam",
    "batch_size": 32,
    "epochs": 10,
    "validation_split": 0.3
  }
}